  # cat=basic//10; type=boolean; label= Enable Web>File: This enables the old Web>File module which shouldn't be needed anymore when using the DAM. Keep in mind that the Web>File module manage files without notifying the DAM about changes which generates inconsistencies.
web_file = 0
  # cat=basic//10; type=boolean; label= Hide Media sysfolder: This adds global user TSConfig which hides the Media sysfolder for all users (options.hideRecords.pages = xx).
hideMediaFolder = 0
  # cat=basic//10; type=boolean; label= Developer Functions: This activates special functions for development. This should stay deactivated for normal usage!
devel = 0
  # cat=basic//10; type=boolean; label= Debug Functions: This activates special functions for debuging. This should stay deactivated for normal usage!
debug = 0